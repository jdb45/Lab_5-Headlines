from setuptools import setup

setup(name='Web Scraping',
    version='0.1',
    description='Getting headlines from ESPN',
    author='jeremy',
    install_requires=['BeautifulSoup', 'requests', 'datetime'])
